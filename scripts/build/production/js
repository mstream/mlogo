#!/usr/bin/env sh

set -E -e

app_path="/mlogo/"

mkdir -p static/

cp src/js/register_sw.js static/
sed -i'.bak' "s#APP_PATH#'${app_path}'#g" static/register_sw.js 
rm static/register_sw.js.bak

cp src/js/sw.js static/

